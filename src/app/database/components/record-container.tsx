import styled from 'styled-components';
import ArrayContainer from './array-container';

const RecordContainer = styled(ArrayContainer)`
    border: 0.25rem solid green;
`;

export default RecordContainer;
