import styled from 'styled-components';
import ArrayContainer from './array-container';

const OptionsContainer = styled(ArrayContainer)`
    border: 0.25rem solid blue;
`;

export default OptionsContainer;
